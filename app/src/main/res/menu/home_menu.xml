<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_search"
        android:title="@string/search_button_text"
        android:icon="@drawable/white_search_button"
        android:orderInCategory="1"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionLayout="@layout/search_view_layout"/>
    <item android:id="@+id/phone"
        android:orderInCategory="0"
        android:icon="@drawable/home_phone_context_button"
        android:title="@string/qr_button_text"
        app:showAsAction="ifRoom|collapseActionView"/>
</menu>